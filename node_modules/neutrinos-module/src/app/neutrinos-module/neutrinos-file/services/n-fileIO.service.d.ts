import { HttpClient } from '@angular/common/http';
export declare class NFileIOService {
    private http;
    systemService: any;
    appProperties: any;
    constructor(http: HttpClient);
    private getFileInfo(options);
    private getFormData(fileUri);
    getPicture(cameraOptions: any): Promise<{}>;
    scanPicture(scanOptions: any): Promise<{}>;
    getBarcode(barcodeOptions: any): Promise<{}>;
    getVideo(videoOptions: any): Promise<{}>;
    getTts(ttsOptions: any): Promise<{}>;
    getShake(shakeOptions: any): Promise<{}>;
    getOcr(ocrOptions: any): Promise<{}>;
    getFingerprint(fingerprintOptions: any): Promise<{}>;
    upload(options: any): Promise<any>;
    download(options: any): Promise<any>;
    saveFile(data: Blob, filename: string): Promise<any>;
    private saveToBrowser(data, fileName);
    private createDirectory(rootDirectory, appName, fileName, data);
    private checkFileExist;
    private setHeaders(headerJSON);
}
