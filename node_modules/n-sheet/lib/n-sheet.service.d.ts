import { HttpClient } from '@angular/common/http';
export declare class NSheetService {
    private http;
    constructor(http: HttpClient);
    getSheet(sheetOptions?: any, htmlContent?: any): Promise<{}>;
    executeCallback(sheetOptions: any): Promise<{}>;
}
